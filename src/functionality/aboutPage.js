//******************************************************************************SETUP ABOUT PAGE
function buildAboutPage() {
    buildBlank();
    //add buttons
    addElmt(buttonContainer);
    //turn on specific buttons
    addElmt(homeButtonContainer);
    addElmt(gamesButtonContainer);

    //build page
    addElmt(aboutPageContainer);
}
