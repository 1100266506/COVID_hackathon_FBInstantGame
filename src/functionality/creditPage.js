//******************************************************************************SET UP CREDIT PAGE
function buildCreditsPage() {
    buildBlank();
    //add buttons
    addElmt(buttonContainer);
    //turn on specific buttons
    addElmt(homeButtonContainer);

    //build page
    addElmt(creditsPageContainer);
}
