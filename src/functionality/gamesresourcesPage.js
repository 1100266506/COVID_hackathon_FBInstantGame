//******************************************************************************SHOWING GAMES AND RESOURCES
function buildGamesResourcesPage() {
    //blank page
    buildBlank();

    //turn on page
    addElmt(gamesresourcesContainer);

    //turn on buttons
    addElmt(buttonContainer);
    addElmt(homeButtonContainer);
}
